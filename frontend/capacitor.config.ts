import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.awaygame.app',
  appName: 'Away Game',
  webDir: 'dist/frontend/browser'
};

export default config;
